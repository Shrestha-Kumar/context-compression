# Context Compression Module - Backend Dependencies
# Pinned to versions verified to work on:
#   - RTX 4050 (6GB VRAM, CUDA 12.1+, INT4 via bitsandbytes)
#   - Kaggle P100 (16GB, CUDA 12.x)

# Core inference stack
torch>=2.3.0,<2.6.0
transformers>=4.44.0,<4.50.0
accelerate>=0.33.0
bitsandbytes>=0.43.0
sentencepiece>=0.2.0
tokenizers>=0.19.0

# Agent orchestration
langgraph>=0.2.0,<0.3.0
langchain-core>=0.3.0,<0.4.0

# Web server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0

# Utilities
pydantic>=2.7.0
